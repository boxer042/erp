# E2E 테스트 설정 — 이 파일을 .env.e2e 로 복사하고 값을 채우세요.
# .env.e2e 는 git 에 커밋되지 않습니다.

# 테스트에 사용할 로그인 계정 (개발/스테이징 환경 계정 권장)
E2E_EMAIL=
E2E_PASSWORD=

# 테스트 대상 주소 — 생략 시 http://localhost:3000
# E2E_BASE_URL=http://localhost:3000
